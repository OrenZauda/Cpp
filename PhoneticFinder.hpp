#include <string>
#include <iostream>
#include <vector>
using namespace std;

namespace phonetic{
    void gen(vector<string>  &arr,int index,string word);
    string find(string text, string word);
    
}
